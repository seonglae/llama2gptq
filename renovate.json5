{
  schedule: 'after 12pm on Thursday',
  extends: ['config:base', 'group:allNonMajor'],
  labels: ['dependencies'],
  rangeStrategy: 'bump',
  pin: false,
  pip_requirements: { fileMatch: ['requirements*.lock'] },
  dependencyDashboardTitle: 'deps: renovate dependency dashboard',
  branchPrefix: 'deps/',
  commitMessagePrefix: 'deps:',
}
